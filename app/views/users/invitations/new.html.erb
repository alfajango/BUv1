<%### users/invitations/new %>
<div class="wrapper body">
  <div class="container">
    <div class="row">
      <%= render "users/signed_in_sidebar" %>  
      <div class="col-sm-8">
		<h2><%= t "Set up a colleague in #{current_user.company.name}'s Prietary" %></h2>

		<%= form_for resource, :as => resource_name, :url => invitation_path(resource_name), :html => {:method => :post} do |f| %>
		  <%= devise_error_messages! %>

			<%# resource.class.invite_key_fields.each do |field| -%>
			<p><%= f.label "Name:" %> 
			<%= f.text_field :name, :autofocus => true %></p>
			<p><b><%= "Email: (must be @#{current_user.company.domain})" %></b>
			<%= f.email_field :email, placeholder: "...@#{current_user.company.domain}" %></p>
			<%# following line doesn't do what I need it to %>
			<%= f.hidden_field :company_id, :value => current_user.company.id %>
			  <!-- <p><%#= f.label field %><br /> -->
			  <!-- <%#= f.text_field field %></p> --> 
			<%# end -%>

		  <p><%= f.submit "Setup your colleague in Prietary" %>
		  <%#= f.submit t("devise.invitations.new.submit_button") %></p>
		<% end %>
	  </div>
	</div>
  </div>
</div>

