<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "BumptUp" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "BumptUp" %>">
    <%= stylesheet_link_tag "application", media: "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <!-- <header> -->
      <%= render 'layouts/header' %> 
      <!-- <%#= render 'layouts/navigation' %> --> <!-- little header -->
    <!-- </header> -->
    <!-- <body> -->
       <%= render 'layouts/messages' %>
       <%= yield %> <!-- yields to home/index.html.erb -->
       <%= debug(params) if Rails.env.development? %>
       <%= render 'layouts/footer' %>

    </body>
<!--   </body> -->


<%#= render 'layouts/landing_page' %> <!-- divshot landing page -->


</html>