<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Prietary" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Prietary" %>">
    <%= stylesheet_link_tag "application", media: "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <!-- <header> -->
      <%= render 'layouts/header' %> 
      <%#= render 'layouts/navigation' %>  <!-- little header -->
    <!-- </header> -->
    <!-- <body> -->
       <%#= render 'layouts/messages' %> <%# todo: put this back in, and get it out from under navbar %>
       <%= yield %> <!-- yields to home/index.html.erb -->
       <%= render 'layouts/footer' %>
       <%= debug(params) if Rails.env.development? %>
    </body>
<!--   </body> -->


<%#= render 'layouts/landing_page' %> <!-- divshot landing page -->


</html>