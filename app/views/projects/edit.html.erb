<div class="wrapper body">
  <div class="col-sm-6 col-sm-offset-3">
    <% provide(:title, 'Edit project') %>
    <h1>Edit Project</h1>

    <%#= form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => { :method => :put }) do |f| %>
    <%= form_for(@project) do |f| %><%# is @project defined? %>
    <%# todo: put error message rendering here %>
    <p><%= f.label "Edit project name:"%><br />
    <%= f.text_field :name %></p>
      <div><%= f.submit "Update", class: "btn btn-primary"%></div> <%# calls update method in projects_controller? %>
    <% end %>
    <br />
    <br />
    <% if current_user === (@project.user) %>
      <h2> or </h2>
      <h1>Delete Project</h1>
    <%= link_to "delete", @project, method: :delete,
                                     data: { confirm: "Are you sure you want to delete the project?  All feedback will be lost" },
                                     title: @project.name, class: "btn btn-danger"%>
<% end %>
    </br></br></br>
    <%= link_to "Back", :back %>
  </div>
</div>