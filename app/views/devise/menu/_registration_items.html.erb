<% if user_signed_in? %>
  <li>
  <%= link_to(current_user.name + " | Settings", edit_user_registration_path) %>
  </li>
<% else %>
  <li>
  <%= link_to('Sign Up' , new_user_registration_path)  %>
  <%#= button_to('Sign up', new_user_registration_path, :method => :get, class: "btn btn-large btn-primary", style:"text-align:center;") %>
  </li>
<% end %>